## 53.

Представьте, что вы пишете программу для запуска космического корабля. Создайте цикл `while`,
который выводит на экран обратный отсчёт от `3` до `1`. Каждое число должно быть на новой строке.
После этого выведите сообщение `"Старт!"`.

Требования:

* В программе должен использоваться цикл `while` для организации обратного отсчёта.
* Цикл `while` должен выводить числа `3`, `2` и `1` в указанном порядке, каждое на новой строке.
* После завершения цикла на экран должно быть выведено сообщение `"Старт!"`.
* Каждое число и сообщение `"Старт!"` должны выводиться с новой строки.

## 54.

Вы пишете программу для разблокировки смартфона. Пользователь должен ввести правильный пароль.
Запрашивайте у пользователя ввод пароля, пока он не введёт слово `"java"`. Как только пароль введён
верно, выведите на экран `"Смартфон разблокирован!"`.

Требования:

* Программа должна использовать цикл `while` для повторного запроса пароля у пользователя.
* На каждой итерации цикла программа должна запрашивать у пользователя ввод пароля с клавиатуры.
* Введённое пользователем значение должно сравниваться со строкой `"java"`.
* Цикл должен завершиться только тогда, когда пользователь введёт `"java"`.
* После выхода из цикла программа должна вывести на экран сообщение `"Смартфон разблокирован!"`.

## 55.

Вы управляете кинотеатром и хотите вывести список доступных мест в ряду. Используйте цикл `while`,
чтобы вывести на экран все номера кресел, начиная со `2`, с шагом в `2`, до `10` включительно.
Выведите каждое число на новой строке.

Требования:

* В задаче должен использоваться цикл `while` для перебора номеров кресел.
* Перебор номеров кресел должен начинаться со второго кресла, то есть с номера `2`.
* При каждом проходе цикла к номеру кресла должно прибавляться `2`.
* Перебор должен выполняться до десятого кресла включительно, то есть до номера `10`.
* Каждый номер кресла должен выводиться на новой строке.

## 56.

Представьте, что вы записываете, сколько тратите на кофе каждый день. Напишите программу, которая
запрашивает стоимость каждой чашки. Ввод должен продолжаться до тех пор, пока вы не введёте
отрицательное число, например `-1`, как сигнал к остановке. Затем программа должна вывести общую
сумму, потраченную на кофе. Отрицательное число в подсчёт не включается.

Требования:

* Программа должна использовать цикл `while` для многократного запроса стоимости кофе у
  пользователя.
* Ввод должен завершаться, когда пользователь вводит отрицательное число, например `-1`.
* Отрицательное число, введённое для завершения, не должно учитываться в общей сумме.
* Программа должна корректно суммировать все введённые неотрицательные значения стоимости кофе.
* После завершения ввода программа должна вывести на экран общую сумму, потраченную на кофе.

## 57.

Вы пишете программу для праздничного обратного отсчёта. Создайте цикл `for`, который выводит числа
от `5` до `1` в обратном порядке. Каждое число должно выводиться на новой строке. После завершения
цикла выведите строку `"С Новым годом!"`.

Требования:

* В программе должен быть использован цикл `for` для организации обратного отсчёта.
* Цикл `for` должен выводить числа от `5` до `1` включительно в обратном порядке.
* Каждое число должно выводиться на новой строке.
* После завершения цикла на экран должна быть выведена строка `"С Новым годом!"`.

## 58.

Вам нужно вывести номера всех чётных мест в вагоне поезда, с `0`-го по `10`-е включительно. Создайте
цикл `for`, который выведет на экран эти номера. Каждое число должно быть на новой строке.

Требования:

* Для вывода номеров мест должен использоваться цикл `for`.
* Цикл должен перебирать номера мест от `0` до `10` включительно.
* На экран должны выводиться только чётные номера мест из указанного диапазона.
* Каждое чётное число должно выводиться на новой строке.

## 59.

Представьте, что у вас есть список из `N` товаров. Пусть переменная `N` содержит количество товаров,
например `7`. Вам нужно посчитать общую стоимость, если цена каждого товара равна его порядковому
номеру: первый — `1`, второй — `2` и так далее. Создайте цикл `for`, который вычислит и выведет
общую сумму.

Требования:

* В программе должна быть переменная `N`, которая хранит количество товаров.
* Для подсчёта суммы должен использоваться цикл `for`.
* Цикл `for` должен проходить по всем товарам от `1` до `N` включительно.
* На каждой итерации к общей сумме должна добавляться текущая стоимость товара, равная его
  порядковому номеру.
* После завершения цикла программа должна вывести общую стоимость всех товаров.

## 60.

Представьте, что вы отслеживаете два объекта на экране. Один движется вправо, а другой — влево.
Создайте один цикл `for`, который выводит пары координат: первое число увеличивается от `1` до `5`,
а второе уменьшается от `5` до `1`. На каждой строке выводите обе координаты через пробел.

Требования:

* Для решения задачи должен быть использован цикл `for`.
* Цикл должен начинаться с первого объекта на позиции `1` и второго объекта на позиции `5` и
  выполняться до тех пор, пока первый объект не достигнет `5`, а второй — `1`.
* В теле цикла обе координаты должны изменяться одновременно: первая увеличивается на `1`, вторая
  уменьшается на `1`.
* На каждой итерации цикла программа должна выводить пару координат через пробел в одной строке.
* Программа должна вывести ровно `5` строк.

## 61.

Представьте, что ваша игра должна выполнить действие хотя бы один раз при первом запуске, независимо
от условий. Создайте переменную `int gameVersion` и присвойте ей значение `10`.
Используйте цикл `do-while`, чтобы вывести на экран `"Загрузка данных..."`,
а затем значение `gameVersion`. Тело цикла должно выполниться ровно один раз:
условие продолжения при первой проверке должно быть ложным.

Требования:

* В программе должна быть объявлена переменная `int` с именем `gameVersion`.
* Переменной `gameVersion` должно быть присвоено значение `10`.
* Для выполнения действий должен использоваться цикл `do-while`.
* В теле цикла `do-while` должно быть выведено сообщение `"Загрузка данных..."`.
* В теле цикла `do-while` должно быть выведено значение переменной `gameVersion`.
* Тело цикла `do-while` должно выполниться ровно один раз;
  условие продолжения при первой проверке должно быть ложным.

## 62.

Вы разрабатываете интерфейс для банкомата. Банкомат должен запрашивать PIN-код, пока пользователь не
введёт четырёхзначное положительное число. Используйте цикл `do-while` и `Scanner`, чтобы программа
запрашивала ввод, пока пользователь не введёт число от `1000` до `9999` включительно. После ввода
корректного числа выведите на экран текст `"PIN-код принят"`.

Требования:

* Программа должна использовать цикл `do-while` для организации повторного запроса PIN-кода.
* Для ввода данных с клавиатуры должен использоваться класс `Scanner`.
* Введённый PIN-код должен быть четырёхзначным положительным числом от `1000` до `9999`
  включительно.
* Программа должна повторять запрос PIN-кода до тех пор, пока пользователь не введёт корректное
  четырёхзначное положительное число.
* После ввода корректного PIN-кода программа должна вывести на экран сообщение `"PIN-код принят"`.

## 63.

Реализуйте простое текстовое меню для командной строки с помощью `do-while`. Программа должна
выводить на экран:

```text
Меню:
1. Вывести приветствие
2. Выйти
```

Пользователь вводит номер пункта меню. Если выбран пункт `1`, программа выводит `"Привет!"` и снова
показывает меню. Если выбран пункт `2`, программа завершает работу. Для ввода используйте `Scanner`.

Требования:

* Меню должно отображаться с помощью цикла `do-while`, чтобы повторять показ меню до выбора выхода.
* В каждом цикле программа должна выводить на экран текст меню с двумя пунктами:
  `"1. Вывести приветствие"` и `"2. Выйти"`.
* Для ввода номера пункта меню программа должна использовать класс `Scanner`.
* Если пользователь выбирает пункт `1`, программа должна вывести `"Привет!"`.
* Если пользователь выбирает пункт `2`, программа должна завершить работу.

## 64.

Вы создаёте форму регистрации, где пароль должен быть не короче `6` символов. Запросите у
пользователя ввод пароля с помощью `Scanner`. Используйте цикл `do-while`, чтобы программа повторно
запрашивала пароль, пока введённая строка короче `6` символов. После ввода корректного пароля
программа должна вывести `"Пароль надёжен, аккаунт создан!"`.

Требования:

* Программа должна использовать класс `Scanner` для считывания пароля пользователя с клавиатуры.
* Для многократного запроса пароля должен использоваться цикл `do-while`.
* Введённый пароль должен проверяться на длину: если он короче `6` символов, запрос должен
  повторяться.
* После ввода корректного пароля длиной не менее `6` символов программа должна вывести
  `"Пароль надёжен, аккаунт создан!"`.

## 65.

Представьте, что вы взламываете замок с кодовым набором. Вам нужно перебрать все комбинации от `1`
до `10`. Как только вы найдёте верный код — `5`, программа должна вывести
`"Код найден! Замок открыт"` и немедленно остановиться.

Требования:

* Программа должна использовать цикл для перебора чисел от `1` до `10` включительно.
* В каждом проходе цикла программа должна сравнивать текущую комбинацию с числом `5`.
* Как только найден верный код, программа должна немедленно выйти из цикла с помощью оператора
  `break`.
* При нахождении верного кода программа должна вывести на экран строку `"Код найден! Замок открыт"`.
* После вывода сообщения программа не должна продолжать перебор оставшихся комбинаций.

## 66.

Вы проверяете партию товаров, и некоторые из них оказались бракованными. Бракованные товары
представлены отрицательными числами. У вас есть массив оценок товаров:

```
{3, -2, 7, -5, 8, 0}
```

Используйте цикл `for`, чтобы вывести на экран только качественные товары: положительные числа или
`0`. Бракованные товары нужно пропустить.

Требования:

* В задаче должен быть использован цикл `for` для перебора элементов массива.
* В программе должен быть массив с оценками товаров, содержащий значения `{3, -2, 7, -5, 8, 0}`.
* На экран должны выводиться только положительные числа или ноль из массива, то есть только
  качественные товары.
* Для пропуска бракованных товаров — отрицательных чисел — необходимо использовать оператор
  `continue` внутри цикла.

## 67.

Пользователь вводит с клавиатуры целое число `n` больше `1`. Ваша задача — проверить, является ли
это число простым. Простое число делится только на `1` и на само себя. Используйте цикл `for`, чтобы
найти первый делитель этого числа от `2` до `n - 1`. Как только делитель найден, выведите его на
экран и завершите цикл. Если делитель не найден, выведите `"Число простое"`.

Требования:

* Программа должна считать с клавиатуры целое число `n` больше `1`.
* Для поиска делителя числа `n` необходимо использовать цикл `for`, который перебирает значения от
  `2` до `n - 1` включительно.
* Если найден делитель числа `n`, то есть `n` делится на `i` без остатка, программа должна вывести
  это значение `i` на экран.
* После нахождения первого делителя программа должна немедленно завершить цикл с помощью оператора
  `break`.
* Если цикл завершился и не было найдено ни одного делителя, программа должна вывести на экран
  сообщение `"Число простое"`.

## 68.

Вы фиксируете продажи за день. Вводите суммы каждой продажи с клавиатуры до тех пор, пока не введёте
отрицательное число (что означает конец рабочего дня). Как только вы вводите отрицательное число,
программа должна показать итоговую сумму всех продаж и завершиться. **Отрицательное число в общую
сумму не включается.**

Требования:

* Программа должна использовать цикл для ввода сумм продаж с клавиатуры до тех пор, пока не будет
  введено отрицательное число.
* Если пользователь вводит отрицательное число, цикл должен завершиться, и это число не должно
  учитываться в итоговой сумме.
* В итоговую сумму должны включаться только положительные значения продаж, введённые до появления
  отрицательного числа.
* После завершения ввода программа должна вывести на экран сумму всех введённых положительных чисел.
* Программа должна корректно считывать и обрабатывать значения, введённые пользователем с
  клавиатуры.

## 69.

Представьте, что вы программируете радар. Напишите программу, которая с помощью вложенных циклов
`for` выводит на экран таблицу координат размером `3×4`. Для каждой ячейки выведите её координаты в
формате `i,j`, где `i` — номер строки (от `0` до `2`), `j` — номер столбца (от `0` до `3`). После
вывода каждой строки переходите на новую строку.

Требования:

* В программе должны быть использованы два вложенных цикла `for` для перебора строк и столбцов
  таблицы.
* Внешний цикл должен перебирать значения `i` от `0` до `2` (всего 3 строки), а внутренний цикл —
  значения `j` от `0` до `3` (всего 4 столбца).
* Для каждой ячейки таблицы на экран должны выводиться её координаты в формате `i,j` (например,
  `1,2`).
* После вывода всех координат в одной строке таблицы программа должна переходить на новую строку.

## 70.

Вы управляете космическим кораблём и хотите визуализировать поле астероидов. Напишите программу,
которая с помощью вложенных циклов `for` выводит на экран прямоугольник из символов `⭐`.
Прямоугольник должен быть размером `5` строк на `7` столбцов. Разделяйте символы пробелом. После
вывода каждой строки переходите на новую строку.

Требования:

* В программе должны использоваться два вложенных цикла `for` для вывода прямоугольника.
* Прямоугольник должен состоять из 5 строк по 7 столбцов.
* В каждой строке между символами `'⭐'` должен быть пробел.
* После вывода каждой строки прямоугольника программа должна переходить на новую строку.

## 71.

Вы разрабатываете игру, и вам нужно создать игровое поле `8x8`. Напишите программу, которая с
помощью вложенных циклов `for` выводит на экран шахматную доску. Используйте символ `"_"` для белых
клеток и `"#"` для чёрных. Клетка считается белой, если сумма номера строки и столбца чётная, иначе
— чёрной. После вывода каждой строки переходите на новую строку.

Требования:

* Программа должна использовать два вложенных цикла `for` для перебора строк и столбцов игрового
  поля.
* Игровое поле должно состоять из 8 строк и 8 столбцов.
* Для каждой клетки цвет определяется по сумме индексов строки и столбца: если сумма чётная — клетка
  белая, если нечётная — чёрная.
* Белые клетки должны обозначаться символом `"_"`, чёрные — символом `"#"`.
* После вывода всех клеток очередной строки программа должна переходить на новую строку.

## 72.

Вы пытаетесь найти идеальный пароль, состоящий из пары целых чисел.
Идеальным считается тот, сумма чисел которого равна `13`.
Напишите программу, которая ищет первую пару целых чисел `i` и `j` (от `1` до
`10`), сумма которых равна `13`. Используйте вложенные циклы `for`. Как только пара найдена,
выведите на экран `"Пароль найден: i=..., j=..."` (подставьте найденные значения) и сразу же
завершите оба цикла, чтобы не продолжать поиск.

Требования:

* Программа должна использовать два вложенных цикла `for` для перебора значений `i` и `j` от `1` до
  `10` включительно.
* Внутри вложенных циклов программа должна проверять, равна ли сумма `i + j` числу `13`.
* При нахождении первой подходящей пары `i` и `j` программа должна вывести строку
  `"Пароль найден: i=..., j=..."`, подставив найденные значения.
* После нахождения первой подходящей пары программа должна немедленно завершить выполнение обоих
  циклов и прекратить дальнейший поиск.
